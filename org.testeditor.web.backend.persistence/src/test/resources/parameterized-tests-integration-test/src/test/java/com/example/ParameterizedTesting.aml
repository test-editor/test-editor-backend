package com.example

import com.example.DummyFixture

component type ParameterizedTestingType {
	interactions = load
}

interaction type load {
	template = "init test data"
	method = DummyFixture.loadDemoData()
}

component ParameterizedTesting is ParameterizedTestingType {}
